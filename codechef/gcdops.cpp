#include <bits/stdc++.h>
using namespace std;
#define boost ios::sync_with_stdio(false); cin.tie(0)

int main() {
    boost;
    int testcase;
    cin >> testcase;
    while (testcase --) {
        int length;
        cin >> length;
        int sequence[length];
        for (int i = 0; i < length; i++) {
            cin >> sequence[i];
        }
        int originalSeq[length];
        for (int i = 0; i < length; i++) {
            originalSeq[i] = i + 1;
        }

        // for (int i = 0; i < length; i++) {
        //     if ()
        // }


    }
    return 0;
}